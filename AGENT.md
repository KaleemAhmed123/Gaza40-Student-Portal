# Agent Instructions → see CLAUDE.md

The authoritative agent guide for this repository is **[CLAUDE.md](CLAUDE.md)** (architecture, folder
structure, data-model gotchas, async/realtime systems, RBAC, conventions, workflow). Claude Code and
most agents auto-load `CLAUDE.md`; this file is kept only as a redirect for tools that look for `AGENT.md`.

Also read before coding:
- `ENGINEERING.md` — code style, simplicity, security rules.
- `vision/requirements.md` — product source of truth (MVP scope).
- `documentation/` — architecture, auth-rbac, database-schema, flows, async-and-realtime.
- `skills/` — architecture and RBAC skills.

Workflow rules (change audit in `plans/changes_audit.md`, Postman regeneration, scope discipline,
discuss-before-deciding) are documented in CLAUDE.md §10.
